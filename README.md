**анализатора арифметических выражений**
Маняхина Евгения Михайловна  
КМБО-02-23 
Вариант: 1  


Программа поддерживает:
- Арифметические операции: `+`, `-`, `*`, `/`  
- Скобки `()`  
- Унарный минус  
- Числа с плавающей точкой и экспоненциальной записью (например, `1.5e2`)  

## Грамматика, используемая в программе
<Expr> ::= <LeadMinusOpt> <Sum>
<LeadMinusOpt> ::= "-" | ε
<Sum> ::= <Prod> <SumTail>
<SumTail> ::= "+" <Prod> <SumTail> | "-" <Prod> <SumTail> | ε
<Prod> ::= <Primary> <ProdTail>
<ProdTail> ::= "*" <Primary> <ProdTail> | "/" <Primary> <ProdTail> | ε
<Primary> ::= "(" <Expr> ")" | NUMBER

## Сборка

Программа написана на C++17
```bash
g++ main.cpp -std=c++17 -o lab5
.\lab5
-(2.2 + 3*4) / 2
^Z
```
